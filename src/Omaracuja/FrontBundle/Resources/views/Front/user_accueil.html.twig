<div class="col-md-10">  
    <div class="row">
        <div class="col-lg-1">
        </div>
        {{ form_start(newBlogPostForm, {'attr': {'class': ''} }) }}
        <div class="col-lg-8">
            {{ form_widget(newBlogPostForm.content, {'attr': {'class': 'form-control', 'placeholder': 'Ajouter un Article'} }) }}
        </div>
        <div class="col-lg-1">
            <input type="submit" class="btn btn-default btn-success text-right" value="Enregistrer" />
        </div>
        {{form_end(newBlogPostForm)}}
    </div>
</div>
